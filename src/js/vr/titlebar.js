export default class TitleBar {
	constructor() {
		var name = "New Panel",
			user_id = "user_id",
			data = {},
			mesh = null;

		 return {
			name: name,
			user_id: user_id,
			data: data,
			mesh: mesh
		 }
	}
}
